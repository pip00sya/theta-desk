# DEVLOG — самопроверки и исправления по ходу разработки

Журнал того, что план обещал, а реализация опровергла или уточнила.
Каждая запись: что нашли → что изменили.

## #1 — `exec` как имя пакета
План называл модуль исполнения `src/exec/`. `exec` — встроенное слово
Python; пакет с таким именем провоцирует конфликты имён.
**Фикс:** пакет называется `execution/`.

## #2 — Хедж не уменьшает worst case defined-risk книги (математика)
План говорил «Hedge закрывает хвост». Тест показал точнее: у книги из
кондоров убыток — плато (width − credit) ниже длинного страйка; дальний
OTM-пут добавляет свою премию к убытку на этом плато и уменьшает потери
только **за** своим страйком. Worst case с хеджем чуть ХУЖЕ (на премию),
зато экстремальный хвост (−20%) превращается из −$2,640 в +$3,240.
**Фикс:** тест переписан на истинное свойство; формулировка в WRITEUP —
«хедж конвертирует экстремальный хвост», а не «уменьшает worst case».
Гейт g8 корректен автоматически: он ценит книгу целиком вместе с хеджем.

## #3 — Случайные бары в моке дают нестабильную реализованную волу
20-точечная выборка случайного блуждания имеет ~16% стандартной ошибки —
тесты режимов флапали. **Фикс:** мок строит детерминированный путь с
чередующимися ±σ лог-доходностями; RV калибрована точно.

## #4 — Идемпотентность была устроена неправильно
Счётчик attempt рос на каждый тик → повторный тик получал НОВЫЙ
client_order_id → в реальном paper это задвоило бы позицию. Первая линия
защиты — не client_order_id, а дедупликация по structure_id (хеш
день|тип|ноги) в сторе ДО гейтов. **Фикс:** повторный кандидат того же дня
журналируется как `entry_skipped_duplicate` и не доходит до исполнения;
attempt инкрементируется только при осознанном репрайсе. Плюс дедуп
кандидатов в shadow_nogates.

## #5 — Флэт-бюджет хеджа (0.6% эквити) пережимал книгу из 1 кондора
Демо-неделя: Core +$223, real-книга −$41 — хедж на $570 премии съел
заработок. План и говорил «хедж финансируется тетой Core», но реализация
привязала бюджет к эквити. **Фикс:** бюджет хеджа =
min(0.6% эквити, 30% собранных кредитов Core) — растёт вместе с книгой.
После фикса: real +$197 при Core-only +$223 — хедж стоит $26.

## #6 — Снапшоты для реплея перезаписывались
Имя файла с точностью до секунды: 8 тиков за 3 секунды → 1 выживший файл,
и реплей спаривал вход с чужим решением. **Фикс:** микросекунды в имени +
журнальная запись `signals` несёт имя своего снапшота; реплей спаривает
строго по ссылке. Результат: 8/8 MATCH.

## #7 — Минимальная жизнеспособная позиция
Живой рынок (SPY@769, VRP 0.33 = cheap) показал: множитель 0.25 в ветке
cheap-vol сжимал бюджет ниже цены одного контракта → size_zero всегда,
ветка была мёртвой. **Фикс:** 1 контракт разрешён, если его риск влезает в
бюджет конструкции (гейт #7 всё равно ограничивает жёстко); множители
масштабируют ВВЕРХ от минимума, не вниз.

## #8 — Streamlit держал sqlite: wipe молча не сработал
Windows не удаляет открытый файл; `-ErrorAction SilentlyContinue` проглотил
ошибку — демо-структуры и счётчики загрязнили живой прогон (ложный отказ
g9 по «дневному риску»). **Фикс:** превью остановлен перед очисткой, wipe
теперь с верификацией «файл исчез». Урок: демо и живое состояние надо
разводить по разным каталогам.

## #9 — Dry-run блокировал боевой вход
Dry-run записывал конструкцию как `open` → боевой тик считал её дубликатом
и пропускал вход при пустом брокере. **Фикс:** dry-run пишет статус
`dry_run`; дедуп и книга его игнорируют.

## Проверено на живом счёте (2026-08-28, вечер ET)
- Paper-гейт, эквити ровно $100,000, **account ID PA39C10YAMYQ**
- Options Level 3 в paper — подтверждено (`options_trading_level: 3`)
- Чейн SPY 2026-09-18 на indicative: 642 контракта, 493 с греками и IV
- **Одноногий лимитник по mid исполняется:** buy 1 SPY260918P00754000
  limit 3.78 → filled @ 3.77. Первая живая сделка агента на судейском
  счёте, полный след решения в журнале
- Реальный режим: RV 13.9% > IV 11.6% → cheap → агент купил вегу, а не
  продал премию. Система следует сигналу, а не нарративу плана

## Инфраструктура (2026-08-29)
- **Alpaca CLI v0.0.14 установлен** (winget Go + `go install`), авторизация
  env-ключами проверена (`alpaca account get` → PA39C10YAMYQ). Следующие
  ордера пойдут транспортом `cli` вместо REST-fallback.
- **Windows Task Scheduler «theta-desk-heartbeat»** зарегистрирован: каждые
  30 минут, обёртка пропускает выходные/нерабочие часы, вечером сохраняет
  evidence-архив. Тестовый запуск отработал полный тик.

## #10 — Featherless: гейтед модель Llama-3.1-70B
Дефолтная модель `meta-llama/Meta-Llama-3.1-70B-Instruct` на Featherless
требует привязки HuggingFace-аккаунта организации (403
`model_gated_needs_oauth`) — это заняло бы отдельную настройку. Проверил
21846 моделей на `/v1/models`, протестировал 5 кандидатов напрямую.
**Фикс:** `Qwen/Qwen2.5-72B-Instruct` (Second Opinion) и `Qwen/Qwen2.5-7B-Instruct`
(News Vetoer) — открытые, без гейта, отвечают 200 и валидным JSON.
Проверено вживую: Second Opinion независимо подтвердил режим `cheap`
при VRP 0.33, совпав с детерминированным сигналом.

## #11 — Все роли переведены на Featherless (2026-08-29)
Пользователь решил не пополнять Anthropic — Vol Analyst и Risk Officer
переехали на открытые модели. Независимость пары «аналитик ↔ второе
мнение» обеспечена разными СЕМЕЙСТВАМИ (Qwen2.5-72B vs Mistral-Small-24B),
а не разными промптами на одних весах. Риск-офицер получил собственные
config-ключи (`risk_provider`/`risk_model`) вместо хардкода на модель
аналитика. Добавлена авторотация на запасной ключ при 401/402/403/429.
Живой тест: 4/4 роли отвечают, 0 fallback; риск-офицер дал severity=high
→ size_mult 0.5 — механизм ужатия размера работает.

## #12 — Знаковая конвенция цены mleg: главный улов недели (2026-08-29)
Субботний acceptance-тест mleg на живом счёте: ордер «продать кондор,
кредит ≥ 3.40» исполнился МГНОВЕННО при закрытом рынке по кредиту 1.79.
Разбор показал: у Alpaca `limit_price` в mleg **знаковый** —
отрицательный = получаемый кредит, положительный = платимый дебет.
Наш `abs()` превращал «получить ≥ 3.40» в «заплатить ≤ 3.40» — любая
котировка удовлетворяла лимиту. В бою агент отдавал бы кондоры по любой
цене без защиты лимита.
**Фикс:** `build_mleg_payload` считает направление денег
(кредит/дебет × открытие/закрытие) и выставляет знак; +3 юнит-теста.
**Подтверждение:** после фикса закрывающий ордер по 1.79 дебета честно
повис в `new` (закрытие стоит ~1.9 через спред) и исполнился только после
поднятия лимита до 2.00 → филл 1.85.
Попутные факты: (а) mleg принимается через CLI-транспорт вместе с
client_order_id; (б) paper исполняет опционные ордера и при закрытом рынке
— по последним пятничным котировкам; (в) тестовая пара ордеров
(td-mleg-selfcheck-001 / -close2) нетировалась в −$6 — цена пересечения
спреда, счёт возвращён к чисто агентской книге.

## #13 — Гибрид включён: Claude на глубоких ролях (2026-09-01)
Пользователь пополнил Anthropic ($5, расчётный расход ~$1.5 до конца).
Vol Analyst и Risk Officer → claude-sonnet-5; Second Opinion (Mistral) и
News Vetoer (Qwen) остаются на Featherless. Пара «аналитик ↔ второе
мнение» теперь МЕЖПРОВАЙДЕРНАЯ — сильнейшая форма сигнала расхождения.
Немедленно видимый эффект качества: возражение Claude-риск-офицера на
текущую книгу — «IV ниже RV, длинная премия борется с отрицательным
carry; дрейф вверх одновременно съедает тету всех трёх путов» —
экономически корректное, в отличие от прежних путаных возражений 24B.
Попутно: ключ был вычищен из .env при отключении — восстановлен.

## #14 — Планировщик на ноутбуке: батарея молча гасит запуски (2026-09-01)
Первое утро нового расписания (старт 18:30, каждые 15 мин): запуски
18:30–19:00 не состоялись. Причина — дефолты Task Scheduler:
DisallowStartIfOnBatteries=true (+ зависший экземпляр держал очередь).
**Фикс:** -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
-MultipleInstances IgnoreNew; зомби-экземпляр снят Stop-ScheduledTask.
ops/schedule_task.ps1 синхронизирован.

## #15 — Аудит «до идеала»: экзиты для дебетовых структур (2026-09-01)
Самая серьёзная находка вторника: менеджер позиций был написан только
для КРЕДИТНЫХ конструкций. Вся текущая книга (длинные путы, дебет) не
имела ни профит-таргета, ни политики реализации — прибыль +$300 никогда
не была бы зафиксирована кодом. Исправления одним пакетом:
1. Дебетовые экзиты: таргет +60% от дебета; стоп-лосса нет сознательно
   (убыток ограничен премией; см. решение по halt-mode).
2. Политика реализации распространена на дебетовые структуры (>=25%).
3. Гейт #18: потолок открытого дебета long-premium рукава 1.5% эквити —
   иначе cheap-ветка добавляла бы по путу на каждый новый страйк дня.
4. Окно NFP-разгрузки 16ч -> 24ч: прежнее начиналось ПОСЛЕ закрытия
   четверга (мёртвое правило); теперь накрывает четверг с полудня ET.
   В окне менеджер ФИКСИРУЕТ позиции с прибылью >= +15%, а не только
   блокирует новые входы.
5. Марки: эквити брокера + греки книги ($-дельта/тета/вега) каждый тик —
   дашборд получил панель атрибуции «почему кривая двинулась».
6. tools/daily_note.py: агент пишет вечернее ретро дня (Claude по
   журналу) — материал для постов и дашборда; параметры НЕ меняет.
+9 тестов (56 всего).

## Anthropic — не код, а биллинг
`ANTHROPIC_API_KEY` авторизуется, но аккаунт без кредитов
(400 `Your credit balance is too low`). Vol Analyst и Risk Officer падают
в fallback и это честно журналируется — деск не ломается, просто теряет
два из четырёх голосов. Чинится пополнением на console.anthropic.com →
Plans & Billing, вне досягаемости кода.

## Открытые допущения
- Поведение филлов mleg-лимитников в paper — одноногий лимитник по mid
  исполняется (проверено), мультиноговый тест — в понедельник на открытии.
- `client_order_id` принимается в mleg-пейлоаде через CLI `alpaca api`.
- Минуты от открытия аппроксимируются через next_close (390-минутная
  сессия); в короткие сессии оценка сместится — некритично из-за гейта
  последних 15 минут, но стоит заменить на календарь.
